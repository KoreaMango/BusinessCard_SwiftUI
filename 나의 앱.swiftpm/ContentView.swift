import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, world!")
                        .padding()
                    Text("This is a custom view")
                        .foregroundColor(.blue)
                        .padding()
        }
    }
}
